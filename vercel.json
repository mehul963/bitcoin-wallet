{
    "builds": [{
        "src": "bitcoin_wallet/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bitcoin_wallet/wsgi.py"
        }
    ]
}
