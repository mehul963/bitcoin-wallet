{
    "builds": [{
        "src": "bitcoin_wallet/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "50mb","version":"python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "bitcoin_wallet/wsgi.py"
        }
    ]
}
